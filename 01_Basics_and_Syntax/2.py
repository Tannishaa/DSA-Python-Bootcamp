# how  to take input in different types
# take age and height inputs from the user in different data types and then print them with type
# python takes input in str by default

age = int(input("enter age: "))
height = float(input("enter height: "))
print(age, type(age))
print(height,type(height))